var name = 'Joshua';
var age = 2;
var hasHobbies = true;

function summarizeUser(UserName, userAge, userHasHobby) {
  return (
    'Name is ' +
    userName +
    ', age is ' +
    userAge +
    ' and has hobbies ' +
    userHasHobby
  );
}

console.log(summarizeUser(name, age, hasHobbies));
